#include "pch.h"
#include "geometry_selector.h"
#include "level_geometries.h"

[[nodiscard]] auto geometry_selector::Get(const player_ship& object) -> ID2D1Geometry*
{
  return level_geometries::get(object);
}

[[nodiscard]] auto geometry_selector::Get(const level_target& object) -> ID2D1Geometry*
{
  return level_geometries::get(object);
}

[[nodiscard]] auto geometry_selector::Get(const mine& object) -> ID2D1Geometry*
{
  return level_geometries::get(object);
}

[[nodiscard]] auto geometry_selector::Get(const level_asteroid& object) -> ID2D1Geometry*
{
  return level_geometries::get(object);
}

[[nodiscard]] auto geometry_selector::Get(const duct_fan& object) -> ID2D1Geometry*
{
  return level_geometries::get(object);
}
